<div class="container">
  <div class="jumbotron jtron">
    <h1>List Of Registered Members</h1>
     <table>
        <tr><th>Name of a Teacher</th><tr>
        <% users.forEach(function(user) { %>   
          <tr><td><%= user.firstName %>  <%= user.lastName %></td></tr>
        <% });  %>
     </table>
  </div>
</div>
