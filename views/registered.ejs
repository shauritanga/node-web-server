<div class="container">
  <div class="jumbotron jtron">
    <h1>List Of Registered Members</h1>
     <table>
        <tr><th>Name of a Teacher</th><tr>
        <% users.forEach(function(user) { %>   
          <tr><td><%= user.firstName.toUpperCase() %>  <%= user.lastName.toUpperCase() %></td></tr>
        <% });  %>
     </table>
  </div>
</div>
